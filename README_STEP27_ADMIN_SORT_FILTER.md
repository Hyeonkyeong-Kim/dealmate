# DealMate Step27 Admin Sort/Filter Fix

## 변경 사항

1. 리뷰확인 화면
- 별점 정렬은 드롭다운으로 유지
  - 낮은 별점순
  - 높은 별점순
- 저평점 사용자 현황 카드는 은은한 주황색 공지 카드로 유지
- 저평점 사용자 현황 카드를 누르면 제재 필요 사용자만 모아보기로 이동

2. 유저관리 화면
- 별점 정렬은 드롭다운으로 변경
  - 낮은 별점순
  - 높은 별점순
- 제재 상태 필터는 화면에 바로 보이는 버튼으로 유지
  - 전체
  - 24시간 제한
  - 계정 정지

3. 유저관리 버튼 규칙
- 전체 상태에서는 [24시간 제한] [계정 정지]
- 24시간 제한 필터에서는 [계정 정지] [해제하기]
- 계정 정지 필터에서는 [24시간 제한] [해제하기]

## 실행

```powershell
cd C:\Users\User\Desktop\dealmate
.\build.bat
java -jar dealmate.jar
```

접속:

```text
http://localhost:8080/login
```

관리자 계정:

```text
admin / admin123
```
