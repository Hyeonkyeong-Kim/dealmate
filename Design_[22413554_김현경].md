<h1 align="center">DEALMATE</h1>

<h3 align="center">Community co-purchases</h3>

<p align="center">
<img width="672" height="257" alt="logo" src="https://github.com/user-attachments/assets/4eb2677f-dcd9-4341-aa3b-6f0d421670f6" />
</p>
<p align="center">
22413554 <br/>  김현경 <br/> 2120khk@naver.com(
rhdiddl7899@yu.ac.kr)<br/>
https://github.com/Hyeonkyeong-Kim/dealmate
</p>

---
<br/><br/><br/>
---
<h2 align="center">Revision History</h2>

| Revision date | Version # | Description | Author |
|:-------------:|:--------:|:-----------:|:------:|
|    26-06-03   |   1.0    | First Draft   | 김현경 |
|    26-06-12   |   1.1    | user/admin flow description update | 김현경 |
|             |          |             |        |
|             |          |             |        |
|             |          |             |        |
|             |          |             |        |
---

<h2 align="center">= Contents =</h2>

1. Introduction ..........................................................................................

2. Class diagram ........................................................................................

3. Sequence diagram ..................................................................................

4. State machine diagram ............................................................................ 

5. Implementation requirements ...................................................................

6. Glossary ....................................................................................................

7. References .................................................................................................

---

## 1. Introduction
과거에는 음식이나 물건을 구매할 때 오프라인으로 구매하였지만 최근에는 식사와 생필품 모든 것을 온라인으로 해결하는 추세이다. 특히 온라인에서는 배달비 뿐만 아니라 대량 구매 가격 대비 소량 구매시 가격 차이가 큰 편이다. 이러한 변화는 1인 가구가 늘어나면서 생활용품이나 식료품을 구매할 때 경제적으로 부담을 느끼게 된다. 특히나 대용량 상품이나 묶음 상품은 가격이 저렴하지만, 혼자 사용하기에는 양이 너무 많거나 보관이 어려운 문제가 있다. 반대로 소량으로 구매하면 가격이 비싸기 때문에 경제적으로 손해를 보는 경우도 있다. 이러한 이유에서 비롯되어 공동구매라는 소비 문화가 생겨나게 되는데 여기서 말하는 공동구매는 구매를 원하는 사람들끼리 모여 동일한 상품을 다량으로 구매하여 물류 비용을 절감하는 장점을 가진다. 더불어 1인 가구가 늘어남에 따라 공동구매는 물건에만 국한되지 않고 음식을 배달할 때 발생하는 최소금액을 충족하기 위해 이루어지기도 한다. 특히 이러한 공동구매는 SNS에서 자주 볼 수 있는데 아래의 사진 2개를 보면 다량의 물건을 구매할 때 저렴한 금액으로 구매가 가능하다보니 친구 혹은 인근에 거주하는 사람들끼리 나눠서 구매하기를 원하는 게시글과 댓글을 보고 이러한 과정을 더 간편하게 실현할 수 있도록 프로그램을 구축하고자 한다.

하지만 공동구매는 여러 사람이 함께 참여하며 돈이 오가기 때문에 관리가 필요하다. 이러한 관리는 참여 인원, 정산 금액, 실결제 금액, 정산 완료 등을 정확하게 확인하지 않으면 사용자 간에 오해가 생길 수 있다. 또한 온라인에서 모르는 사람과 거래하는 경우에는 신뢰 문제가 발생할 수 있기 때문에 같은 지역 사용자끼리 안전하게 공동구매를 진행할 수 있는 시스템이 필요하다고 생각하였다.

DealMate는 앞서 말한 문제를 해결하기 위한 지역 기반 공동구매 플랫폼이다. 동네 인증을 통해 같은 동네의 사용자들과 특가 정보를 공유할 수 있고 필요한 경우 공동구매 방을 직접 생성하거나 참여할 수 있다. 공동구매가 완료된 후에는 호스트가 결제 영수증을 업로드하여 실결제를 인증하고 시스템은 공동구매에 참여 인원 수에 맞게 정산 금액을 계산한다. 공동구매에 참여한 멤버는 정산 금액 송금 인증 이미지를 업로드해 정산 인증을 하며 거래가 끝난 후에는 사용자 간의 리뷰를 작성한다. 이러한 절차는 사용자 신뢰를 높이고 공동구매 과정에서 발생할 수 있는 결제 금액 오류나 정산 누락을 줄이기 위해 마련된다.

이 시스템을 통해 이루고자 하는 첫 번째 목표는 공동구매 과정을 편리하게 관리하는 것이다. 공동구매 방 생성, 참여자 확인, 정산 요청, 송금 인증과 같은 기능을 하나의 프로그램 안에서 처리할 수 있도록 하여 다수의 사용자가 쉽게 접근할 수 있도록 한다. 두 번째 목표는 안전한 거래 환경을 제공하는 것이다. 이를 구현하기 위해서는 동네 인증, 영수증 인증, 송금 인증, 리뷰 기능을 통해 투명한 금액 관리가 가능하도록 하고 그 과정에서 문제를 발생시킨 사용자는 관리자가 확인하고 제재 조치를 할 수 있도록 한다. 주 타겟층은 대용량 상품을 혼자 구매하기 부담스러운 1인 가구, 구매하고 싶은 것이 같은 사용자들이다. 또한 단순히 공동구매만 이루어지는 게 아닌 지역 커뮤니티로써 사용자에게도 유용하게 사용될 수 있다.

<p align="center">
  <img width="45%" alt="image" src="https://github.com/user-attachments/assets/ddfb839f-dfea-4f3a-8409-2a05b6fc8962" />
  <img width="50%" alt="image" src="https://github.com/user-attachments/assets/3869bd2f-d6bd-4c27-8f8c-22b731f205a5" />
</p>

---
## 2. Class diagram
<img width="1399" height="761" alt="image" src="https://github.com/user-attachments/assets/0936e993-a24d-4683-98d2-ca09fc56ff9e" />

사용자는 기본적으로 User로 정의하고, 공동구매 방을 생성하는 Host, 공동구매에 참여하는 Member, 사용자를 관리하는 Administrator로 구분하였다.

## [표 2-1] Class Description

| Class Name                | Attributes                                                                                                                          | Methods                                                                                                                                           | Description                                                                                               |
| ------------------------- | ----------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------- |
| User                      | userId:String, password:String, email:String, averageRating:double                                                                  | login(userId:String, password:String):boolean, logout():void, writePost(title:String, content:String):void                                        | DealMate를 사용하는 일반 사용자의 공통 정보를 관리하는 부모 클래스이다. 일반 사용자는 특가 정보 게시글 및 확인 등 전반적인 활동을 할 수 있으며 Host와 Member 클래스의 공통 정보를 제공한다. |
| Host                      | createdRooms:ArrayList<GroupPurchaseRoom>, settlementList:ArrayList<Settlement>                                                     | createGroupPurchase(room:GroupPurchaseRoom):void, requestSettlement(roomId:int):void, uploadPaymentReceipt(receiptImage:String):void              | User를 상속받는 호스트 역할 클래스이다. 공동구매 방을 생성할 수 있고 결제 영수증을 업로드하며 정산 요청 기능을 수행한다.                                     |
| Member                    | joinedRooms:ArrayList<GroupPurchaseRoom>, transferProofList:ArrayList<TransferProof>                                                | joinGroupPurchase(roomId:int):boolean, checkSettlementDetails(roomId:int):void, uploadTransferProof(transferImage:String):void                    | User를 상속받는 멤버 역할 클래스이다. 개설된 공동구매 방에 참여하고, 호스트의 결제 내역을 확인하며, 송금 인증 이미지를 업로드한다.                                        |
| Administrator             | managedUsers:ArrayList<User>, managementList:ArrayList<AdministratorManagement>                                                     | showLowRatingUsers():ArrayList<User>, restrictUser(userId:String):void, suspendUser(userId:String):void                                           | 일반 사용자를 관리할 수 있는 관리자 클래스이다. 관리자는 리뷰확인 화면에서 평균 별점과 리뷰 수를 확인하고, 유저관리 화면에서 저평점 사용자에게 24시간 활동 제한 또는 계정 정지와 같은 관리 조치를 수행한다. 제재가 해제된 경우 계정 상태는 정상으로 돌아간다. |
| Registration              | userId:String, password:String, email:String                                                                                        | register(user:User):boolean, checkDuplicateId(userId:String):boolean, checkRequiredFields(user:User):boolean                                      | 회원가입 정보(아이디, 비밀번호, 이메일)를 입력받고 필수 입력 여부와 아이디 중복 여부를 확인한 뒤 사용자 정보를 등록하는 클래스이다.                                              |
| Login                     | loginStatus:boolean, currentUser:User                                                                                               | login(id:String, pw:String):boolean, logout(user:User):void, validatePassword(id:String, pw:String):boolean, isAdministrator(id:String):boolean   | 일반 사용자와 관리자의 로그인 및 로그아웃을 처리하는 클래스이다.                                                                      |
| NeighborhoodCertification | location:String, neighborhood:String, certificationStatus:boolean                                                                   | requestCertification(location:String):boolean, verifyLocation(location:String):boolean, saveNeighborhood(userId:String, neighborhood:String):void | 사용자의 위치 또는 선택 지역을 확인하여 동네 인증 정보를 저장하는 클래스이다.                                                              |
| ScreenController          | currentScreen:String, loginUser:User, isAdminMode:boolean                                                                           | changeScreen(screenName:String):void, showPopup(message:String):void, showUserNavigation():void, showAdminNavigation():void                       | Java 기반 UI 구현에서 화면 전환과 팝업 출력을 담당하는 제어 클래스이다. 일반 사용자 로그인 시 사용자 화면을 제공하고, 관리자 로그인 시 리뷰확인, 유저관리, 마이로 구성된 관리자 화면을 제공한다. |
| Database                  | userTable:ArrayList<User>, postTable:ArrayList<Post>, roomTable:ArrayList<GroupPurchaseRoom>, settlementTable:ArrayList<Settlement> | saveData(object:Object):void, findUser(userId:String):User, findRoom(roomId:int):GroupPurchaseRoom                                                | 사용자, 게시글, 공동구매 방, 정산 정보를 저장하고 조회하는 저장소 역할의 클래스이다.               |
| Post                      | postId:int, writerId:String, title:String, content:String                                                                           | writePost(title:String, content:String):void, savePost():void                                                                                     | 메인 화면에 표시되는 지역 특가 정보 게시글을 관리하는 클래스이다. 제목과 내용은 필수 입력값이며, 사진 첨부는 선택사항으로 처리된다. 시연 구현에서는 실제 이미지 파일 저장 대신 이미지 첨부 여부와 이미지 미리보기 안내 문구를 화면에 표시한다. |
| GroupPurchaseRoom         | roomId:int, hostId:String, productName:String, maxParticipants:int, currentParticipants:int                                         | createRoom(productName:String, max:int):void, showRoomList():ArrayList<GroupPurchaseRoom>, updateParticipantCount():void                          | 공구 탭에서 생성할 수 있는 공동구매 방 정보를 관리하는 클래스이다. Host가 공동구매 방을 생성하며, maxParticipants는 호스트를 포함한 전체 최대 참여 인원으로 사용된다. 화면에서는 총 예상 결제 금액을 기준으로 1인당 예상 금액을 함께 표시하여 사용자가 참여 전 금액을 확인할 수 있도록 한다. |
| GroupParticipation        | participationId:int, roomId:int, userId:String                                                                                      | joinRoom(userId:String, roomId:int):boolean, checkAvailableSeat(roomId:int):boolean                                                               | 특정 사용자가 특정 공동구매 방에 참여했다는 기록을 저장하는 중간 클래스이다. 새로운 사용자 유형이 아니라 Member와 GroupPurchaseRoom 사이의 참여 관계를 관리한다. 참여 가능 여부 확인 시 모집 인원 초과뿐 아니라 중복 참여와 호스트 본인의 자기 방 참여를 함께 제한한다. |
| PaymentReceipt            | receiptId:int, roomId:int, receiptImage:String, ocrAmount:int, finalAmount:int                                                      | uploadReceipt(receiptImage:String):void, extractAmountByOCR(receiptImage:String):int                                                              | Host가 업로드한 결제 영수증 이미지와 총 결제 금액 검증 정보를 관리하는 클래스이다. OCR을 통해 금액을 인식하고, 시연 구현에서는 인식 실패 또는 금액 오류 상황을 고려하여 호스트가 직접 입력한 금액을 정산 계산에 사용할 수 있도록 한다. |
| Settlement                | settlementId:int, roomId:int, totalAmount:int, participantCount:int, amountPerPerson:int                                            | calculateAmount(totalAmount:int, count:int):int, updateSettlementStatus(status:String):void                                                       | 선택된 정산 대상 공구의 총 결제 금액과 참여 인원수를 기준으로 1인당 정산 금액을 계산하고 정산 상태를 관리하는 클래스이다. 호스트가 직접 입력한 총 결제 금액이 변경되면 정산 카드의 총 결제 금액과 1인당 정산 금액이 함께 갱신된다. |
| TransferProof             | proofId:int, userId:String, transferImage:String                                                                                    | uploadTransferProof(transferImage:String):void, updateProofStatus(status:String):void                                                             | Member가 업로드한 송금 인증 이미지와 인증 상태를 관리하는 클래스이다.                                                                |
| Review                    | reviewId:int, writerId:String, targetUserId:String, rating:int                                                                      | writeReview(rating:int, content:String):void, updateAverageRating():void                                                                          | 공동구매 완료 후 작성되는 리뷰와 별점을 저장하여 평균 별점에 반영하는 클래스이다. 사용자는 리뷰 대상자를 선택한 뒤 별점과 리뷰 내용을 입력하며, 리뷰 저장 후 대상 사용자의 평균 별점이 갱신된다. |
| AdministratorManagement   | managementId:int, adminId:String, targetUserId:String, actionType:String                                                            | showLowRatingUsers():ArrayList<User>, applyManagementAction(actionType:String):void                                                               | 관리자가 특정 사용자를 선택하고 24시간 활동 제한, 계정 정지, 제재 해제와 같은 관리 조치를 적용하는 클래스이다. 관리자는 리뷰확인 화면에서 평균 별점과 리뷰 수를 확인하고, 유저관리 화면에서 낮은 별점순/높은 별점순 정렬 및 계정 상태 필터를 사용하여 관리 대상을 확인한다. 관리 대상은 User(Host와 Member)이다. |


---
## 3. Sequence diagram
### 1) Register
<img width="684" height="545" alt="스크린샷 2026-06-04 194111" src="https://github.com/user-attachments/assets/f62300df-88cf-4b8b-8f88-a3a51bb6d34a" />

main에서 사용자가 회원가입을 실행하면 Registration 클래스의 register(user) 메소드가 호출된다.
Registration 클래스에서는 사용자가 입력한 회원가입 정보(유저 아이디, 비밀번호,이메일)가 모두 입력되었는지 확인하기 위해 checkRequiredFields(user) 메소드를 실행한다.
그다음 입력된 userId가 이미 사용 중인 아이디인지 확인하기 위해 checkDuplicateId(userId) 메소드를 실행한다.
checkRequiredFields(user)의 결과가 true이고 checkDuplicateId(userId)의 결과가 false이면 회원가입이 가능한 상태로 판단한다.
회원가입 조건을 만족하면 Registration 클래스는 Database 클래스의 saveData(user) 메소드를 호출하여 사용자 정보를 저장한다.
Database 클래스는 저장 처리가 끝난 뒤 saveData() 응답을 Registration 클래스에게 반환한다.
Registration 클래스는 register() 응답을 main에게 반환하여 회원가입 흐름을 완료한다.
필수 입력값이 누락되었거나 아이디가 중복된 경우에는 회원가입을 진행하지 않고 오류 상황으로 처리한다.

### 2) Neighborhood Certification
<img width="658" height="510" alt="스크린샷 2026-06-04 201531" src="https://github.com/user-attachments/assets/35aa9e8c-4796-4d35-8da1-0d4df55d8150" />

ScreenController에서 사용자가 동네 인증을 실행하면 NeighborhoodCertification 클래스의 requestCertification(location) 메소드가 호출된다.
NeighborhoodCertification 클래스는 사용자의 위치 또는 선택 지역을 확인하기 위해 verifyLocation(location) 메소드를 실행한다.
verifyLocation(location)의 결과가 true이면 사용자의 위치가 인증 가능한 지역으로 판단된다.
인증에 성공하면 NeighborhoodCertification 클래스의 saveNeighborhood(userId, neighborhood) 메소드를 통해 사용자의 동네 정보를 저장한다.
그다음 Database 클래스의 saveData(neighborhood) 메소드를 호출하여 동네 인증 정보를 데이터베이스에 저장한다.
저장이 완료되면 ScreenController 클래스의 changeScreen(screenName) 메소드를 통해 다음 화면으로 전환한다.
verifyLocation(location)의 결과가 false이면 동네 인증을 진행할 수 없으므로 ScreenController 클래스의 showPopup(message) 메소드를 호출하여 오류 메시지를 출력한다.

### 3) login

<img width="887" height="674" alt="image" src="https://github.com/user-attachments/assets/85c97a64-aee9-4db2-a63a-7caa9d26abe8" />

main에서 사용자가 로그인을 실행하면 Login 클래스의 login(userId, pw) 메소드가 호출된다.
Login 클래스는 입력된 userId가 존재하는지 확인하기 위해 Database 클래스의 findUser(userId) 메소드를 호출한다.
사용자 정보가 조회되면 Login 클래스의 validatePassword(id, pw) 메소드를 통해 입력한 비밀번호가 올바른지 확인한다.
그다음 Login 클래스의 isAdministrator(id) 메소드를 실행하여 로그인한 사용자가 일반 사용자인지 관리자인지 구분한다.
validatePassword(id, pw)의 결과가 true이고 isAdministrator(id)의 결과가 false이면 일반 사용자 로그인으로 판단한다.
일반 사용자 로그인에 성공하면 ScreenController 클래스의 showUserNavigation() 메소드를 호출하여 일반 사용자용 메뉴를 보여주고, changeScreen(screenName) 메소드를 통해 사용자 메인 화면으로 전환한다.
validatePassword(id, pw)의 결과가 true이고 isAdministrator(id)의 결과가 true이면 관리자 로그인으로 판단한다.
관리자 로그인에 성공하면 ScreenController 클래스의 showAdminNavigation() 메소드를 호출하여 관리자용 메뉴를 보여주고, changeScreen(screenName) 메소드를 통해 관리자 메인 화면으로 전환한다.
비밀번호가 올바르지 않거나 사용자를 찾을 수 없는 경우에는 ScreenController 클래스의 showPopup(message) 메소드를 호출하여 로그인 실패 메시지를 출력한다.

### 4) Write Post
   
<img width="648" height="460" alt="image" src="https://github.com/user-attachments/assets/4595b737-6766-41fa-a981-db3fb7c14554" />

main에서 사용자가 게시글 작성을 실행하면 User 클래스의 writePost(title, content) 메소드가 호출된다.
User 클래스는 Post 클래스의 writePost(title, content) 메소드를 호출하여 게시글 제목과 내용을 전달한다.
Post 클래스에서는 전달받은 title과 content를 이용하여 게시글 정보를 생성한다.
최종 구현에서는 게시글 작성 화면에 제목, 내용 입력 영역과 사진 첨부 선택 영역이 제공된다. 제목과 내용은 필수 입력값이지만 사진 첨부는 선택사항이므로 첨부하지 않아도 게시글 등록이 가능하다.
사진을 첨부한 경우 목록에는 이미지 첨부 표시가 나타나고, 상세 화면에는 이미지 미리보기 영역과 “시연 중에는 실제 이미지 미리보기가 제공되지 않습니다.” 안내 문구가 표시된다.
게시글 정보가 생성되면 Post 클래스의 savePost() 메소드를 실행하여 게시글 저장 처리를 진행한다.
그다음 Post 클래스는 Database 클래스의 saveData(post) 메소드를 호출하여 생성된 게시글 정보를 데이터베이스에 저장한다.
Database 클래스는 저장이 완료되면 saveData() 응답을 Post 클래스에게 반환한다.
Post 클래스는 writePost() 응답을 User 클래스에게 반환한다.
User 클래스는 최종적으로 writePost() 응답을 main에게 반환하여 게시글 작성 흐름을 완료한다.

### 5) Create Group Purchase Room
 <img width="626" height="524" alt="image" src="https://github.com/user-attachments/assets/ce3825b9-730c-4432-b145-359b7decf3de" />

main에서 사용자가 공동구매 방 생성을 실행하면 Host 클래스의 createGroupPurchase(room) 메소드가 호출된다.
Host 클래스는 GroupPurchaseRoom 클래스의 createRoom(productName, max) 메소드를 호출하여 공동구매 방 생성을 요청한다.
GroupPurchaseRoom 클래스에서는 전달받은 productName과 max 값을 이용하여 공동구매 방 정보를 생성한다.
최종 구현에서는 호스트가 상품명, 설명, 최대 참여 인원, 총 예상 결제 금액을 입력하여 공동구매 방을 생성한다. 이때 최대 참여 인원은 호스트를 포함한 전체 인원으로 해석하며, 방 생성 시 호스트가 자동으로 참여 인원에 포함된다.
화면에서는 총 예상 결제 금액을 최대 참여 인원으로 나눈 1인당 예상 금액을 함께 표시하여 참여자가 공구 참여 전에 예상 정산 금액을 확인할 수 있도록 한다.
공동구매 방 정보가 생성되면 GroupPurchaseRoom 클래스는 Database 클래스의 saveData(room) 메소드를 호출하여 방 정보를 데이터베이스에 저장한다.
Database 클래스는 저장이 완료되면 saveData() 응답을 GroupPurchaseRoom 클래스에게 반환한다.
GroupPurchaseRoom 클래스는 createRoom() 응답을 Host 클래스에게 반환한다.
Host 클래스는 최종적으로 createGroupPurchase() 응답을 main에게 반환하여 공동구매 방 생성 흐름을 완료한다.

### 6) Join Group Purchase
<img width="579" height="471" alt="공구방참여(오타수정)" src="https://github.com/user-attachments/assets/39518b39-a6d7-4f79-ac95-604853b176f9" />

main에서 사용자가 공동구매 참여를 실행하면 Member 클래스의 joinGroupPurchase(roomId) 메소드가 호출된다.
Member 클래스는 GroupParticipation 클래스의 checkAvailableSeat(roomId) 메소드를 호출하여 해당 공동구매 방에 참여 가능한 자리가 있는지 확인한다.
GroupParticipation 클래스는 checkAvailableSeat(roomId)를 통해 현재 참여 인원과 최대 참여 인원을 비교한다.
최종 구현에서는 모집 인원 마감 여부뿐 아니라 이미 참여한 사용자인지, 호스트가 본인이 생성한 공구에 참여하려는 경우인지도 함께 확인한다.
checkAvailableSeat(roomId)의 결과가 false이거나 중복 참여 또는 호스트 본인 참여에 해당하면 참여 정보를 저장하지 않고 joinGroupPurchase() 응답을 Member 클래스에게 반환한다.
checkAvailableSeat(roomId)의 결과가 true이면 참여 가능한 상태이므로 GroupParticipation 클래스의 joinRoom(userId, roomId) 메소드를 실행하여 참여 정보를 생성한다.
참여 정보가 생성되면 GroupParticipation 클래스는 Database 클래스의 saveData(participation) 메소드를 호출하여 공동구매 참여 정보를 저장한다.
Database 클래스는 저장이 완료되면 saveData() 응답을 GroupParticipation 클래스에게 반환한다.
GroupParticipation 클래스는 joinRoom() 응답을 Member 클래스에게 반환한다.
Member 클래스는 최종적으로 joinGroupPurchase() 응답을 main에게 반환하여 공동구매 참여 흐름을 완료한다.

### 7) Requset Settlement
 <img width="737" height="548" alt="image" src="https://github.com/user-attachments/assets/1e79423c-371d-49a8-a8d4-dce8f0a1556c" />

메인 화면에서 Host가 공동구매 탭의 공구 상세페이지로 이동하면 자신과 관련된 공동구매 방을 확인할 수 있다.
관련 공구가 여러 개인 경우 화면의 좌우 이동 기능을 통해 정산 대상 공구를 선택한다. 선택된 공구 카드에는 상품명, 호스트, 참여 인원, 총 예상 결제 금액, 1인당 예상 금액, 상태가 표시된다.
Host가 결제 영수증을 업로드하면 Host 클래스의 uploadPaymentReceipt(receiptImage) 메소드가 실행된다. Host 클래스는 PaymentReceipt 클래스에게 uploadReceipt(receiptImage)를 요청하여 이미지를 저장한다.
PaymentReceipt 클래스의 extractAmountByOCR(receiptImage) 메소드는 이미지에서 결제된 금액을 추출한다. OCR로 금액 추출 성공 시 PaymentReceipt 객체의 ocrAmount 값이 finalAmount에 반영되어 Database 클래스의 saveData(receipt) 메소드를 통해 영수증 정보가 저장된다.
시연 구현에서는 금액 인식 실패나 잘못된 인식 상황을 고려하여 Host가 총 결제 금액을 직접 입력할 수 있으며, 입력된 금액은 최종 결제 금액으로 반영된다.
이후 Host 클래스의 requestSettlement(roomId) 메소드를 실행하여 해당 공동구매 방의 정산 요청을 진행한다.

### 8) Calculate Settlement Amount
<img width="690" height="518" alt="image" src="https://github.com/user-attachments/assets/093b4fa2-0f22-4535-aca2-190c5379cc1f" />

main에서 Host에게 정산 요청을 보내면 Host 클래스의 requestSettlement(roomId) 메소드가 실행된다.
Host 클래스는 Settlement 클래스의 calculateAmount(totalAmount, count) 메소드를 호출하여 총 결제 금액과 참여 인원수를 전달한다.
Settlement 클래스에서는 전달받은 totalAmount와 count를 이용하여 1인당 정산 금액을 계산한다.
최종 구현에서는 선택된 정산 대상 공구의 참여 인원수를 기준으로 계산하며, 호스트가 직접 입력한 총 결제 금액을 변경하면 정산 카드의 총 결제 금액과 1인당 정산 금액이 함께 갱신된다.
계산이 완료되면 updateSettlementStatus(status) 메소드를 통해 정산 상태를 갱신한다.
참여 인원수가 0보다 큰 경우 Database 클래스의 saveData(settlement) 메소드를 호출하여 계산된 정산 정보를 저장한다.
Database는 저장이 완료되면 saveData() 응답을 반환한다.
Settlement는 calculateAmount() 응답을 Host에게 반환하고, Host는 requestSettlement() 응답을 main에게 반환한다.
참여 인원수가 0 이하이거나 총 결제 금액이 올바르지 않은 경우에는 정산 금액을 계산할 수 없으므로 오류 처리한다.

### 9) Receipt Verification
 <img width="697" height="503" alt="결제내역인증 호스트(오타수정)" src="https://github.com/user-attachments/assets/f6083fdf-1bf2-4944-98f9-f306b1a08652" />

main에서 Host가 정산 요청을 실행하면 Host 클래스의 requestSettlement(roomId) 메소드가 호출된다.
Host 클래스는 uploadPaymentReceipt(receiptImage) 메소드를 통해 결제 영수증 이미지를 업로드한다.
Host 클래스는 PaymentReceipt 클래스의 uploadReceipt(receiptImage) 메소드를 호출하여 영수증 이미지를 전달한다.
PaymentReceipt 클래스는 extractAmountByOCR(receiptImage) 메소드를 실행하여 영수증 이미지에서 총 결제 금액을 추출한다.
추출된 총 결제 금액은 Settlement 클래스의 calculateAmount(totalAmount, count) 메소드로 전달된다.
Settlement 클래스는 총 결제 금액과 참여 인원수를 기준으로 1인당 정산 금액을 계산한다.
계산이 완료되면 updateSettlementStatus(status) 메소드를 통해 정산 상태를 갱신한다.
Settlement 클래스는 Database 클래스의 saveData(settlement) 메소드를 호출하여 정산 정보를 저장한다.
Database 클래스는 저장이 완료되면 saveData() 응답을 Settlement 클래스에게 반환한다.
Settlement 클래스는 계산 완료 응답을 Host 클래스에게 반환하고, Host 클래스는 최종적으로 requestSettlement() 응답을 main에게 반환하여 정산 요청 흐름을 완료한다.


### 10) Write Review
 <img width="724" height="503" alt="image" src="https://github.com/user-attachments/assets/fb7fcb3b-0ab3-4672-99d1-98c9eabd2cfb" />

main에서 사용자가 리뷰 작성 기능을 실행하면 User 클래스의 writeReview(rating, content) 메소드가 호출된다.
최종 구현에서는 사용자가 리뷰 대상 사용자를 선택한 뒤 별점과 리뷰 내용을 입력한다. 리뷰 대상, 별점, 내용이 모두 입력된 경우에만 리뷰 등록을 진행한다.
User 클래스는 Review 클래스에게 writeReview(rating, content)를 요청하여 별점과 리뷰 내용을 전달한다.
Review 클래스에서는 전달받은 rating과 content를 이용하여 리뷰 정보를 생성한다.
리뷰 작성 후 Review 클래스의 updateAverageRating() 메소드가 실행되어 대상 사용자의 평균 별점이 갱신된다.
갱신된 리뷰 정보는 Database 클래스의 saveData(review) 메소드를 통해 저장된다.
Database는 저장 처리가 완료되면 saveData() 응답을 Review 클래스에게 반환한다.
Review 클래스는 writeReview() 응답을 User 클래스에게 반환하고, User 클래스는 최종적으로 main에게 writeReview() 응답을 반환한다.

### 11) Manage User

  <img width="724" height="506" alt="image" src="https://github.com/user-attachments/assets/b0a94075-62b2-42d5-b8d2-87e3a8af6c55" />

main에서 관리자가 관리자 계정으로 로그인하면 Login 클래스의 isAdministrator(id) 메소드를 통해 관리자 여부가 확인되고, ScreenController 클래스의 showAdminNavigation() 메소드가 실행된다.
관리자 화면은 리뷰확인, 유저관리, 마이 하단 메뉴로 구성된다.
리뷰확인 화면에서는 Administrator 클래스의 showLowRatingUsers() 메소드와 AdministratorManagement 클래스의 showLowRatingUsers() 메소드를 통해 사용자별 평균 별점, 리뷰 수, 대표 리뷰, 계정 상태를 확인한다.
관리자는 별점 정렬 기능을 통해 낮은 별점순 또는 높은 별점순으로 사용자를 확인할 수 있고, 저평점 사용자 현황 카드를 선택하여 제재가 필요한 사용자만 모아볼 수 있다.
유저관리 화면에서는 전체 사용자, 24시간 활동 제한 사용자, 계정 정지 사용자를 상태별로 필터링하여 확인한다.
관리자가 특정 사용자를 선택하고 관리 조치를 적용하면 AdministratorManagement 클래스의 applyManagementAction(actionType) 메소드가 실행된다.
actionType 값이 "restrict"인 경우 Administrator 클래스의 restrictUser(userId) 메소드를 호출하여 해당 사용자를 24시간 활동 제한 상태로 처리한다.
actionType 값이 "suspend"인 경우 Administrator 클래스의 suspendUser(userId) 메소드를 호출하여 해당 사용자를 계정 정지 상태로 처리한다.
제재 해제의 경우 동일한 관리 조치 흐름을 통해 해당 사용자의 계정 상태를 정상으로 변경한다.
관리 조치가 적용되면 Database 클래스의 saveData(management) 메소드를 통해 관리 조치 정보가 저장되고, 리뷰확인 화면과 유저관리 화면의 계정 상태가 동일하게 반영된다.
Database 클래스는 저장이 완료되면 saveData() 응답을 AdministratorManagement 클래스에게 반환한다.
AdministratorManagement 클래스는 최종적으로 applyManagementAction() 응답을 main에게 반환하여 사용자 관리 조치가 완료되었음을 알린다.

---
## 4. State machine diagram
### 4.1 Client State Machine Diagram
<img width="741" height="650" alt="image" src="https://github.com/user-attachments/assets/fbb12ff2-7d0c-4d35-a5f4-6df71e0e3403" />

### 4.2 Server State Machine Diagram
<img width="1127" height="548" alt="image" src="https://github.com/user-attachments/assets/4272d0bf-1d5a-4db1-b39b-3e8bc982eb1a" />


### 상태 목록 및 설명

| 상태                         | 설명                                                                                                                                                                                                                                                                                             |
| -------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Idle                       | 앱이 실행된 후 사용자의 선택을 기다리는 초기 상태이다. 사용자는 이 상태에서 회원가입 또는 로그인을 선택할 수 있다. 회원가입을 선택하면 Registration 상태로 전이되고, 로그인을 선택하면 Login Verification 상태로 전이된다.                                                                                                                                                    |
| Registration               | 사용자가 회원가입 정보를 입력하는 상태이다. 사용자는 userId, password, email 등의 정보를 입력한다. 회원가입이 성공하면 Neighborhood Certification 상태로 전이되고, 입력 정보가 누락되거나 아이디가 중복되면 Idle 상태로 돌아간다.                                                                                                                                       |
| Neighborhood Certification | 회원가입 이후 사용자의 동네 인증을 수행하는 상태이다. 사용자가 선택한 지역 또는 위치 정보를 기준으로 동네 인증을 진행한다. 동네 인증이 성공하면 Login Verification 상태로 전이되고, 인증에 실패하면 Idle 상태로 돌아간다.                                                                                                                                                        |
| Login Verification         | 사용자가 아이디와 비밀번호를 입력하여 로그인을 시도하는 상태이다. 입력된 로그인 정보를 확인하고 일반 사용자와 관리자를 구분한다. 일반 사용자 로그인에 성공하면 User Session 상태로 전이되고, 관리자 로그인에 성공하면 Admin Session 상태로 전이된다. 로그인에 실패하면 Idle 상태로 돌아간다.                                                                                                                |
| User Session               | 일반 사용자로 로그인된 상태를 나타내는 복합 상태이다. User Session 안에는 Home, Post Writing, Group Purchase List, Group Room Creation, Group Participation, Settlement Management, Review Writing 상태가 포함된다. 일반 사용자는 이 상태에서 특가 정보 글 작성, 공동구매 방 생성, 공동구매 참여, 정산 확인, 리뷰 작성 기능을 사용할 수 있다. logout 이벤트가 발생하면 Logout 상태로 전이된다. |
| Home                       | 일반 사용자가 로그인한 뒤 처음 진입하는 기본 화면 상태이다. 사용자는 홈 화면에서 특가 정보 글을 확인하거나, 글쓰기 또는 공동구매 기능으로 이동할 수 있다. 특가 정보 글 작성을 선택하면 Post Writing 상태로 전이되고, 공동구매 메뉴를 선택하면 Group Purchase List 상태로 전이된다.                                                                                                                  |
| Post Writing               | 사용자가 특가 정보 게시글을 작성하는 상태이다. 사용자는 제목과 내용을 입력하고 선택적으로 사진을 첨부할 수 있다. 사진이 첨부된 글은 목록에서 이미지 첨부 표시가 나타나며, 상세 화면에서는 시연용 이미지 미리보기 안내가 표시된다. 게시글 저장이 완료되면 다시 Home 상태로 돌아간다. |
| Group Purchase List        | 사용자가 공동구매 방 목록을 확인하는 상태이다. 사용자는 모집 중인 공동구매 방을 확인할 수 있고, 방을 직접 생성하거나 기존 방에 참여할 수 있다. Host로 방을 생성하는 경우 Group Room Creation 상태로 전이되고, Member로 방에 참여하는 경우 Group Participation 상태로 전이된다.                                                                                                            |
| Group Room Creation        | Host가 공동구매 방을 생성하는 상태이다. 상품명, 최대 참여 인원, 마감 시간, 총 예상 결제 금액 등의 정보를 입력하여 공동구매 방을 생성한다. 최대 참여 인원은 호스트를 포함한 전체 인원으로 해석되며, 시스템은 1인당 예상 금액을 함께 표시한다. 방 생성이 완료되면 Group Purchase List 상태로 돌아간다. |
| Group Participation        | Member가 공동구매 방에 참여하는 상태이다. 참여 가능 인원을 확인한 뒤 공동구매 방에 참여한다. 이때 모집 인원 마감, 중복 참여, 호스트 본인 참여 여부를 함께 확인한다. 공동구매 참여가 완료되고 구매가 진행되면 Settlement Management 상태로 전이된다. |
| Settlement Management      | 공동구매 완료 후 정산을 확인하고 처리하는 상태이다. Host는 정산 대상 공구를 선택하고 결제 금액을 입력하거나 영수증 인증을 통해 정산을 요청할 수 있으며, Member는 정산 대상 공구의 총 결제 금액과 1인당 정산 금액을 확인하고 송금 인증을 진행할 수 있다. 정산이 완료되면 Review Writing 상태로 전이된다. |
| Review Writing             | 공동구매가 완료된 후 사용자가 리뷰를 작성하는 상태이다. 사용자는 리뷰 대상자를 선택하고 거래 상대방에 대한 별점과 리뷰 내용을 작성한다. 리뷰 저장이 완료되면 대상 사용자의 평균 별점이 갱신되고 Home 상태로 돌아간다. |
| Admin Session              | 관리자로 로그인된 상태를 나타내는 복합 상태이다. Admin Session 안에는 리뷰확인, 유저관리, 마이 상태가 포함된다. 관리자는 리뷰와 평균 별점을 기준으로 사용자를 확인하고, 필요 시 사용자 관리 조치를 수행할 수 있다. logout 이벤트가 발생하면 Logout 상태로 전이된다. |
| Admin Home                 | 관리자가 로그인한 뒤 처음 진입하는 관리자 리뷰확인 상태이다. 관리자는 사용자별 평균 별점, 리뷰 수, 대표 리뷰, 계정 상태를 확인하고 낮은 별점순 또는 높은 별점순으로 정렬할 수 있다. 저평점 사용자 현황 카드를 선택하면 제재가 필요한 사용자만 모아볼 수 있다. |
| User Management            | 관리자가 저평점 사용자를 조회하고 관리 조치를 수행하는 상태이다. 관리자는 전체, 24시간 활동 제한, 계정 정지 상태별 필터를 사용하여 사용자를 확인하고, 사용자 상태에 따라 24시간 활동 제한, 계정 정지, 해제하기 조치를 적용할 수 있다. 관리 조치가 완료되면 변경된 계정 상태가 관리자 화면에 반영된다. |
| Logout                     | 일반 사용자 또는 관리자가 로그아웃을 수행한 상태이다. 로그아웃이 완료되면 클라이언트의 활동이 종료되고 Final State로 전이된다.                                                                                                                                                                                                                   |

---

| 상태                        | 설명                                                                                                                                                                                                                                                                                 |
| ------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Waiting Request           | 서버가 클라이언트 요청을 기다리는 초기 상태이다. 클라이언트에서 회원가입, 로그인, 동네 인증, 게시글 작성, 공동구매, 정산, 리뷰, 관리자 조치 등의 요청이 들어오면 Request Validation 상태로 전이된다.                                                                                                                                                        |
| Request Validation        | 서버가 수신한 요청의 유효성을 검증하는 상태이다. 요청에 필요한 값이 존재하는지, 형식이 올바른지, 로그인 세션이나 권한이 적절한지 확인한다. 요청이 유효하면 Request Processing 상태로 전이되고, 요청이 유효하지 않으면 Error Response 상태로 전이된다.                                                                                                                        |
| Request Processing        | 서버가 요청 종류에 따라 실제 기능을 처리하는 복합 상태이다. Request Processing 안에는 Request Type Check, Account Processing, Post Processing, Group Purchase Processing, Settlement Processing, Admin Processing 상태가 포함된다. 서버는 요청 유형을 확인한 뒤 알맞은 처리 상태로 이동한다.                                                  |
| Request Type Check        | 수신된 요청이 어떤 기능에 해당하는지 확인하는 상태이다. 요청이 회원가입, 로그인, 동네 인증과 관련되어 있으면 Account Processing 상태로 전이된다. 게시글 작성 요청이면 Post Processing 상태로, 공동구매 방 생성 또는 참여 요청이면 Group Purchase Processing 상태로 전이된다. 정산 관련 요청이면 Settlement Processing 상태로, 관리자 조치 요청이면 Admin Processing 상태로 전이된다.                 |
| Account Processing        | 회원가입, 로그인, 동네 인증과 관련된 요청을 처리하는 상태이다. 회원가입 요청이 들어오면 register(user)를 수행하고, 로그인 요청이 들어오면 login(id, pw)를 수행한다. 동네 인증 요청이 들어오면 requestCertification(location)을 처리한다. 처리가 완료되면 Request Processing 내부의 종료 상태로 전이된다.                                                                       |
| Post Processing           | 특가 정보 게시글 작성 요청을 처리하는 상태이다. 사용자가 입력한 제목과 내용을 바탕으로 writePost(title, content)를 수행하고, savePost()를 통해 게시글 저장을 처리한다. 처리가 완료되면 Request Processing 내부의 종료 상태로 전이된다.                                                                                                                       |
| Group Purchase Processing | 공동구매 방 생성 또는 참여 요청을 처리하는 상태이다. Host가 공동구매 방을 생성하는 경우 createRoom()을 수행하고, Member가 공동구매 방에 참여하는 경우 joinRoom()을 수행한다. 참여 인원 변경이 필요한 경우 updateParticipantCount()를 수행한다. 처리가 완료되면 Request Processing 내부의 종료 상태로 전이된다.                                                                   |
| Settlement Processing     | 정산과 관련된 요청을 처리하는 상태이다. Host가 영수증을 업로드하면 uploadReceipt(receiptImage)를 수행하고, OCR 또는 입력값을 통해 결제 금액을 확인한다. 이후 calculateAmount(totalAmount, count)를 통해 1인당 정산 금액을 계산한다. Member가 송금 인증 이미지를 업로드하면 uploadTransferProof(transferImage)를 처리한다. 처리가 완료되면 Request Processing 내부의 종료 상태로 전이된다. |
| Admin Processing          | 관리자의 사용자 관리 요청을 처리하는 상태이다. 관리자는 showLowRatingUsers()를 통해 낮은 별점 사용자를 조회하고, applyManagementAction(actionType)을 통해 24시간 활동 제한, 계정 정지, 제재 해제 조치를 수행한다. 처리가 완료되면 Request Processing 내부의 종료 상태로 전이된다. |
| Database Update           | 요청 처리가 완료된 후 데이터베이스를 갱신하는 상태이다. 회원 정보, 동네 인증 정보, 게시글 정보, 공동구매 방 정보, 참여 정보, 정산 정보, 리뷰 정보, 관리자 조치 정보 등이 저장되거나 수정된다. 데이터 갱신이 완료되면 Response Sending 상태로 전이된다.                                                                                                                          |
| Response Sending          | 서버가 클라이언트에게 처리 결과를 반환하는 상태이다. 요청 처리 성공 또는 실패 결과를 클라이언트로 전달한다. 응답 전송이 완료되면 서버는 다시 Waiting Request 상태로 돌아가 다음 요청을 기다린다.                                                                                                                                                              |
| Error Response            | 요청 검증 실패 또는 처리 오류가 발생했을 때 오류 응답을 생성하는 상태이다. 필수 입력값 누락, 잘못된 로그인 정보, 권한 부족, 잘못된 요청 형식 등이 발생하면 Error Response 상태로 전이된다. 오류 응답을 생성한 뒤 Response Sending 상태로 전이된다.                                                                                                                       |


---

## 5. Implementation Requirements

### 1) Hardware Requirements

| Hardware | Requirement           |
| -------- | --------------------- |
| CPU      | Intel i3 이상 또는 동급 CPU |
| RAM      | 8GB 이상 권장             |
| Storage  | 1GB 이상의 여유 공간         |
| Network  | 로컬 실행 시 필수 아님         |

### 2) Software Requirements

| Software                | Requirement                |
| ----------------------- | -------------------------- |
| Operating System        | Windows 10 이상              |
| Implementation Language | Java 17 이상                 |
| Development Tool        | IntelliJ IDEA 또는 Eclipse   |
| Web Browser             | Chrome 또는 Edge             |
| Data Storage            | 파일 저장, 리스트 자료구조, 또는 간단한 DB |

### 3) Nonfunctional Requirements

본 구현에서는 관리자도 로그인 기능을 통해 시스템에 접근하므로, 코드 구현상 Administrator 클래스가 로그인 계정 정보를 처리하기 위해 User 기반 구조를 사용하였다. 다만 시스템 설계상 Administrator는 일반 사용자와 동일한 공동구매 참여자가 아니라, 사용자 관리 기능을 수행하는 별도의 관리자 역할로 구분된다.

DealMate는 웹 브라우저에서 실행하지만 모바일 앱과 비슷한 비율로 구성하여 앱 구동처럼 보이게 한다. 사용자는 로그인 후 특가 정보 글 작성, 공동구매 방 생성[호스트], 공동구매 참여[멤버], 정산 확인, 리뷰 작성 기능을 사용할 수 있다. 관리자는 관리자 계정으로 로그인한 경우에만 리뷰확인, 유저관리, 마이 화면에 접근할 수 있으며, 사용자 평균 별점과 리뷰 수를 기준으로 24시간 활동 제한, 계정 정지, 제재 해제 조치를 적용할 수 있다.

최종 구현은 Java 내장 HttpServer 기반의 로컬 실행 방식으로 구성되며, dealmate.jar 실행 후 웹 브라우저에서 http://localhost:8080/login 주소로 접속하여 확인할 수 있다. 외부 배포 서버가 아닌 로컬 시연 방식이므로 실행 파일을 실행한 PC에서 서버가 열린다.

시스템은 시연 중 버튼 클릭 후 화면 전환과 결과 출력이 자연스럽게 이루어지도록 구현한다. 로그인, 회원가입, 게시글 작성과 같은 기본 기능은 작동 후 3초 이내에 구현되는 것을 목표로 한다.

---
## 6. Glossary

| Term                        | Description                                                                                                     |
| --------------------------- | --------------------------------------------------------------------------------------------------------------- |
| DealMate                    | 지역 기반으로 특가 정보 공유와 공동구매를 지원하는 시스템 이름이다.                                                                          |
| User                        | DealMate 내 사용자를 의미한다. 정보글 작성, 공동구매 방 생성, 공동구매 참여와 같은 전반적인 기능을 다 할 수 있다.                                        |
| Administrator               | DealMate 시스템을 관리하는 관리자이다. 리뷰확인 화면에서 평균 별점과 리뷰 수를 확인하고, 유저관리 화면에서 24시간 활동 제한, 계정 정지, 제재 해제를 적용할 수 있다. |
| Host                        | 공동구매 방을 생성한 사용자를 의미한다. Host는 공동구매를 진행하고 정산 요청을 수행한다.                                                            |
| Member                      | 공동구매 방에 참여한 사용자를 의미한다. Member는 공동구매에 참여하고 정산 금액 송금 인증을 수행할 수 있다.                                                      |
| Database                    | 사용자 정보, 게시글 정보, 공동구매 방 정보, 정산 정보, 리뷰 정보 등을 저장하는 저장소 역할을 한다. 구현 시에는 파일 저장, 리스트 자료구조, 또는 간단한 데이터베이스로 대체할 수 있다.    |
| ScreenController            | 화면 전환과 팝업 메시지 출력을 담당하는 클래스이다. 사용자의 버튼 클릭에 따라 알맞은 화면으로 이동시킨다.                                                    |
| Composite State             | 여러 개의 하위 상태를 포함하는 큰 상태를 의미한다. DealMate에서는 User Session, Admin Session, Request Processing 등이 이에 해당한다.           |
| 리뷰확인                    | 관리자가 사용자별 평균 별점, 리뷰 수, 대표 리뷰, 계정 상태를 확인하는 관리자 화면이다. 낮은 별점순과 높은 별점순 정렬을 제공하며, 저평점 사용자 현황을 통해 제재 필요 사용자를 모아볼 수 있다. |
| 유저관리                    | 관리자가 사용자 계정 상태를 확인하고 24시간 활동 제한, 계정 정지, 제재 해제를 적용하는 관리자 화면이다. 전체, 24시간 활동 제한, 계정 정지 필터를 제공한다. |
| 계정 상태                   | 관리자 관리 대상 사용자의 현재 상태를 의미한다. 최종 구현에서는 정상, 24시간 활동 제한, 계정 정지로 구분한다. |
| 이미지 첨부                 | 특가 정보글 작성 시 선택적으로 사용할 수 있는 기능이다. 시연 구현에서는 실제 이미지 저장 대신 첨부 여부와 이미지 미리보기 안내를 표시한다. |


---
## 7. References
- https://toss.im/tossfeed/article/group-purchase?srsltid=AfmBOorLoYl0yQGLrEaRxMyPohY983r1bNH83M9loM5pGq59bpc9gm_7
- https://www.hapt.co.kr/news/articleView.html?idxno=154631
- https://docs.oracle.com/javase/tutorial/java/IandI/subclasses.html
- 강의자료 : Structural Modeling2

